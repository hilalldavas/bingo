rules_version = '2';

// TEST İÇİN GEÇİCİ AÇIK KURALLAR - PRODUCTION'DA KULLANMAYIN!
service cloud.firestore {
  match /databases/{database}/documents {
    // Geçici olarak tüm okuma/yazma işlemlerine izin ver (TEST İÇİN)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

// Firebase Storage kuralları
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
